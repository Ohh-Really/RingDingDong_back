spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:test;MODE=MYSQL
    driverClassName: org.h2.Driver
    username: sa

springdoc:
  swagger-ui:
    path: /api-docs